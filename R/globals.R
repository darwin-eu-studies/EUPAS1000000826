# Global variable bindings to avoid R CMD check NOTEs
utils::globalVariables(c(
  "age",
  "age_group",
  "cohort_definition_id",
  "cohort_end_date",
  "cohort_start_date",
  "number_records",
  "number_subjects",
  "person_count",
  "record_count",
  "sex",
  "subject_id"
))
